<!DOCTYPE HTML>
<html ng-app="favoritelist">
    <head>
      <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.14/angular.min.js"></script>
      <script type="text/javascript" src="app.js"></script>
    </head>

    <body>
      <div class="container" ng-controller="FavoriteListController">
        <input type = "text" ng-model= "thingName">
        <input type = "text" ng-model= "thingColor" >
        <input type = "text" ng-model= "thingColor1">
        <input type = "text" ng-model= "activity">
        <input type = "text" ng-model= "personName">

        <hr>
        <br>Your favorite thing is a:
        <div style= "color:{{thingColor}}">
          <h1>
            {{thingName}}
          </h1>
        </div>
        Your favorite color is:
        <div style= "color:{{thingColor1}}">
          <h1>
            {{thingColor}}
          </h1>
        </div>
        <p ng-show = "{{showThis}}">
          Hi {{personName}}! You should go do {{activity}}
        </p>
          <ul>
            <li ng-repeat="x in names">
              {{ x.name }}
            </li>
          </ul>
      </div>
    </body>
</html>
